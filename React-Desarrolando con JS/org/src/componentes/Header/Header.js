import "./Header.css"

function Header(){
    return  <header className="header"> {/* classNAme es la forma de colocar una clase dentro de una etiqueta HTML, se evita el uso de class solo, debido a la palabra reservada en la POO */}
        <img src="/img/HeaderHeader_total.png" alt="Org" />
    </header>
}

export default Header